# §477 — vless `encryption`: проверка грамматики ядра, негодное значение отбраковывает узел

| | |
|---|---|
| **Статус** | **Реализовано (ревизия 1), тесты зелёные (12 своих)** — правило в реестре из лаунчера `12b45282` (контракт 1.1.9), `TASKS_LXBOX.md` §24.19. Исполнено одним заходом с §475. Сужено владельцем до проверки формы; полная грамматика — справка, не реализуется. |
| **Дата** | 2026-09-18 |
| **Источник** | [#147](https://github.com/Leadaxe/LxBox/issues/147); решение владельца 18.09.2026; лаунчер — встречная задача (sha придёт) |
| **Связанные** | фича 472 (vless на конвейере с шага 3), фича 460, §457 (`key_share` — тот же приём «нормативно для пина ядра») |

## Проблема

`encryption` у vless в ядре — закрытая грамматика, а не свободная строка.
Значение, которое грамматике не соответствует, роняет старт **всего
конфига**: одна негодная строка в одном узле подписки — и VPN не поднимается
ни на одном узле. Приложение сегодня значение не проверяет вовсе.

## Решение — ревизия 1 (действующая)

Владелец: «делаем минимальную проверку просто по форме». Дублировать
разборщик ядра не будем — копия расходится с ядром на бампе и тогда
отбраковывает годные узлы.

Правило — регулярное выражение, которое владелец получил от сессии ядра:

`^mlkem768x25519plus(\.[^.]+){3,}$` (с учётом регистра)

Порядок, нормативный для приложения:

1. URL-декодирование параметра ссылки (маппер). Символы `=`, `/`, `+` и пробел
   чаще всего появляются не от автора ключа, а от пропущенного или двойного
   декодирования.
2. Обрезка пробелов ПО КРАЯМ всей строки — так делает и само ядро
   (`strings.TrimSpace`).
3. Результат пуст или точно `none` — поле в тело не кладётся. Ядро сравнивает
   с `none` ДО обрезки и значение ` none` приняло бы за строку шифрования;
   автор имел в виду «без шифрования».
4. Иначе — регулярка. Не прошло — `on_invalid: drop_node`, код
   `vless_encryption_invalid` (error, `params: path + value`, `value` —
   сырое). В тело кладётся ОБРЕЗАННОЕ значение.

Пробелы внутри сегментов регулярка допускает намеренно: ядро тримит сегменты
с четвёртого, и значение `….0rtt. KEY` оно принимает — запрет пробелов
отбраковал бы годный узел. Имя метода зашито: реестр и ядро едут в одном
релизе приложения, новый метод возможен только с новым ядром. В
`docs/KERNEL.md` — строка «при бампе сверить имя метода с
`protocol/vless/lx_encryption.go`».

Вид, RTT, padding, длины и коэффициенты ключей не проверяются — это ловит
фича 478. Правило ловит случай #147 (`got N segments`), пустые сегменты,
хвостовую точку, мусор без имени метода и `None`/`NONE`. Исполняет санитайзер
по телу — одна запись на ссылку, JSON и Xray; как правило выражено в реестре
(`pattern` + обрезка краёв) — решает лаунчер. Критерии приёмки ниже действуют
в объёме этой ревизии (юниты — на форму и порядок обработки).

### Как правило выражено в реестре (контракт 1.1.9)

`vless.body.fields.encryption`: `normalize: "trim"` + `absent_values:
["none"]` + `pattern: "^mlkem768x25519plus(\\.[^.]+){3,}$"` + `on_invalid:
drop_node`, код `vless_encryption_invalid` (error, `params: path + value`,
`value` — сырое, до обрезки).

Два новых ОБЩИХ атрибута схемы — завести оба в `registry.dart`,
`body_sanitizer.dart` и линтере (`registry_invariant_test`):

- **`pattern`** — регулярное выражение; диалект = RE2 ∩ ECMAScript/Dart: без
  lookaround, backreference и inline-флагов, якоря в самом выражении. Линтер
  отвергает выражение вне диалекта (достаточно проверки на запрещённые
  конструкции + компиляция `RegExp`).
- **`absent_values`** — значения-выключатели: поле в тело не кладётся,
  правила его не судят. Без него стороны расходятся на теле с
  `"encryption": "none"`. Сравнение ТОЧНОЕ на всех входах (`None` → негодное
  → `drop_node`) — в ссылке, теле sing-box и Xray-JSON одинаково.

Порядок нормативен: декодировать → обрезать края → пусто / точное `none`? →
`pattern`. Вид нормализации `trim` — проверить, есть ли у нас; нет — завести.
Корпус: 18 кейсов `uri/vless` + `body/singbox`, существующие ожидания не
менялись. У URI-параметра `encryption` снята пометка `ext: "mobile"` —
проверить, не влияет ли на наши чтения реестра и стража mapper-правил.
Xray-JSON вход до шага 8 фичи 472 идёт старым путём — правило на нём
исполнится после переезда; записать в спеку 472 (шаг 8).

## Решение — исходная редакция (не реализуется, справка)

1. Правило живёт в реестре контракта: `format: vless_encryption` у
   `vless.body.fields.encryption`, `on_invalid: drop_node`, код
   `vless_encryption_invalid` (error, `params: path + value`). Пустое
   значение и `none` — как сейчас: поле в тело не кладётся.
   **Узел отбраковывается, поле не снимается:** узел без `encryption` к
   серверу, который его требует, всё равно не подключится, а молча снять
   слой шифрования — тихое понижение защиты.
2. Исполняет санитайзер по ТЕЛУ — одна запись закрывает ссылку,
   JSON-вкладку и (после шага 8 фичи 472) Xray-JSON. Параметр ссылки
   percent-декодирует маппер до проверки.
3. Грамматика — по исходникам ядра (`protocol/vless/lx_encryption.go`,
   `encryption/common.go`), подтверждённая сессией ядра; нормативный текст —
   описание формата в реестре, Dart-реализация совпадает с Go побайтно:

   `mlkem768x25519plus.<native|xorpub|random>.<0rtt|1rtt>[.<padding>…].<key>[.<key>…]`

   - сегментов не меньше 4, пустых нет;
   - padding (0..N до первого ключа): сегмент короче 20 символов, форма
     `int-int-int`; у первого блока ≥100-≥35-≥35; сумма максимумов блоков на
     чётных позициях ≤ 65553;
   - ключей не меньше одного: base64url без `=`, после декода ровно 32 или
     1184 байта;
   - ключ 1184 байта: каждый 12-битный коэффициент первых 1152 байт < 3329
     (то, что в Go делает `mlkem.NewEncapsulationKey768`) — ручной
     распаковкой, без криптобиблиотеки.

   Уточнения ядра (граница padding/ключ, индексация «чётных позиций»,
   регистр, пробелы) вносятся сюда до начала работы.
4. Правило нормативно для пина ядра **lx.8**. В `docs/KERNEL.md` — строка в
   список «сверить при бампе»: `protocol/vless/lx_encryption.go`. Новый метод
   или вид в ядре без правки реестра = годные узлы отбраковываются, поэтому
   сверка обязательна.
5. Синк на коммит лаунчера с форматом (контракт 1.1.9); кейсы корпуса — по
   одному на каждую строку отказа ядра плюс годные (ключ 32, ключ 1184, с
   padding и без, percent-кодированное значение в ссылке).

## Уточнения ядра (сверка по исходникам v1.14.1-lx.8, 18.09.2026)

Источники: `protocol/vless/outbound.go:107`, `lx_encryption.go`
(`parseClientEncryption`), `encryption/common.go` (`ParsePadding`),
`encryption/client.go` (`Init`). Между lx.4 и lx.8 в `protocol/vless/`
коммитов нет. Нормативен текст формата в реестре; здесь — порядок разбора,
который реализация обязана повторить.

0. Точное сравнение ДО trim: `""` и `none` — слой не создаётся. `None`,
   `NONE`, ` none` идут в разбор и отвергаются («1 segments»). Строка из
   пробелов — `empty encryption string`.
1. Вся строка — trim, затем split по `.`; сегментов < 4 — отказ.
2. Сегменты 0–2 НЕ тримятся, сравнение точное, с учётом регистра. Пустой
   сегмент здесь даёт ошибку метода/вида/RTT, не `empty segment`.
3. Сегменты с 4-го — каждый тримится; пустой — `empty segment`.
4. **Padding или ключ — только по длине:** пока не встретился первый ключ и
   длина сегмента (в байтах, после trim) < 20 — это padding, что бы в нём ни
   было; иначе — ключ (base64url без `=`, 32 или 1184 байта = 43 или 1579
   символов), и фаза padding закрыта навсегда. Padding длиной ≥ 20 и короткий
   сегмент после ключа — ветка ключа, отказ по base64/длине. Смешение ключей
   32 и 1184 разрешено; лимитов на длину и число блоков нет.
5. Padding-блок: split по `-`, частей НЕ МЕНЬШЕ трёх (лишние игнорируются),
   первые три непустые, каждая — целое (`Atoi`: ведущий `+` допустим).
6. Первый блок: ≥100, ≥35, ≥35.
7. Блоки с чётным индексом С НУЛЯ (0, 2, 4…) — длины, с нечётным — паузы.
   Сумма `max(y1, y2)` по блокам-длинам ≤ 65553. У пауз — только форма.
8. Ключ 32 байта — любой. Ключ 1184: **768** коэффициентов по 12 бит
   (FIPS 203 ByteDecode₁₂) из первых 1152 байт, каждый < 3329; на 3 байта
   `b0,b1,b2` два значения: `c0 = b0 | (b1 & 0x0F) << 8`,
   `c1 = b1 >> 4 | b2 << 4`. Последние 32 байта не проверяются.
9. Go `RawURLEncoding` молча пропускает `\r`/`\n` внутри сегмента и не
   требует каноничных хвостовых битов. Клиент может быть строже ядра —
   как решит лаунчер в описании формата, так и делаем.

Снаружи отказ выглядит как `initialize outbound[i] vless[tag]: parse
encryption: …` либо `…: initialize encryption: …` — обе роняют старт целиком.

## Критерии приёмки

- корпус зелёный без override;
- юниты на каждую ветку грамматики, включая коэффициент ≥ 3329 и границы
  padding; годные значения из живых подписок (фикстуры app/test с
  `encryption`) проходят — identity-фикстура vless без расхождений;
- узел с негодным `encryption`: отбракован, в списке отбракованных виден код
  с исходным значением; остальные узлы подписки живы;
- узел `origin.kind: json` (дословный, §455): гард сборки тоже не пускает
  негодное значение в ядро — закрыть тестом;
- тесты только затронутых каталогов + прогон без `app/contract`; CI зелёный;
- CHANGELOG (Fixed, со ссылкой на #147), GUARDS.md, PROTOCOLS.md (VLESS —
  encryption), KERNEL.md.

## Вне задачи

Общая страховка «старт упал → ядро назвало узел (SPEC 092) → исключить узел
и стартовать заново» — отдельное обсуждение с владельцем; эта задача —
первый эшелон с понятным текстом.

## Как сделано

### Два общих атрибута реестра

`pattern` и `absent_values` заведены как ОБЩИЕ выражения схемы, не
vless-специфичные: `FieldSchema.pattern` / `FieldSchema.absentValues`
(`registry.dart`), исполнение — скалярная ветка `body_sanitizer.dart`, линтер —
`registry_invariant_test.dart`. Вид нормализации `trim` уже был и в реестре, и в
санитайзере; §477 стал первым потребителем, у которого от ПОРЯДКА зависит исход.

Порядок в коде ровно тот, что нормирован контрактом:

```
_coerceType → normalize (trim) → absent_values → normalize_code
            → values → format/min/max/len → pattern → max_when → advisory
```

`absent_values` стоит ДО остальных ограничений потому, что выключатель — это
отсутствие значения, а не значение: суди его закрытым набором или выражением, и
узел был бы похоронен за выключенную настройку.

`pattern` проверяется по значению, которое ЛЯЖЕТ в тело (после `trim`), а в код
уезжает `coerced.value` — сырое, до обрезки. Причина та же, по которой сырое
значение носит `normalize_code`: человеку нужно видеть, что он написал.

Скомпилированные выражения кешируются по тексту (`_patternCache`): выражений в
реестре единицы, а санитайзер бегает по каждому полю каждого узла подписки.
Некомпилируемое выражение ПРОПУСКАЕТСЯ — реестр вправе ехать впереди кода
(24.1), — и поэтому опечатку обязан ловить линтер, а не рантайм.

### Линтер диалекта

Диалект проверяется не на «компилируется ли», а на общее подмножество RE2 и
Dart: отвергаются lookahead/lookbehind, обратные ссылки `\1`..`\9` и inline-флаги
`(?i)`/`(?m)`/`(?s)`/`(?U)`. Inline-флаг тут самый коварный: RE2 его понимает, а
Dart нет — выражение скомпилировалось бы у обеих сторон, а СМЫСЛ был бы разный,
и стороны разошлись бы на живых узлах молча. Требуются якоря `^`…`$` в самом
выражении: режим «совпасть целиком» стороны задают по-разному.

У `absent_values` линтер требует непустой список строк и запрещает значение,
стоящее одновременно в `values`: поле было бы разом и выключателем, и годным
значением.

### Маппер ссылки похудел

Из `vless_mapper.dart` ушли и обрезка, и сравнение с `none`. Осталось: взять
URL-декодированный параметр и не класть ключ, если параметра не было. Всё
остальное судит санитайзер по ТЕЛУ — одной записью на три входа.

Сравнение с `none` стало ТОЧНЫМ, и это изменение против прежнего поведения:
раньше маппер сравнивал `EqualFold`, то есть `None`/`NONE` считались «слоя нет».
Ядро сличает литерал с учётом регистра, для него `None` — настоящее значение, на
котором падает весь конфиг. Прятать его под видом выключателя значило бы
пропустить негодный узел в ядро.

### Как исполняется `drop_node` на JSON-входе

Это была главная незакрытая часть. Санитайзер по дословной карте
(`annotateFromRawBody`, шаг 1 фичи 472) РАНЬШЕ выбрасывал `res.body` целиком и
брал только коды — то есть вердикт «запись снять» не доезжал никуда. Теперь
функция возвращает вердикт, а `parseAll` убирает такой узел из списка и кладёт
причину в `dropped[]`. Тело узла по-прежнему не меняется: границы шага 1 в силе,
наружу уезжает только вердикт.

`dropped[].ref` контракта — ТЕГ записи (D-088), поэтому код реестра получил
необязательное поле `ownerTag` (`RegistryWarning`). Вне `props` намеренно:
`props` — это идентичность предупреждения, по ней идёт дедуп (§279), а тег
говорит не «что случилось», а «с какой записью». Отдельного класса заводить не
стали: причина отбраковки — сам код реестра с его текстом, и новый класс
потребовал бы новой видимой строки там, где она уже есть.

**Граница, которую пришлось провести отдельно.** Первая версия снимала узел на
любом `res.body == null` — и это оказалось слишком широко: записи теряют тело и
от НЕДОСТАЮЩЕГО обязательного поля (`tuic.uuid` не в форме UUID, 24.1.7), а
такой узел приложение показывает с самого начала и снимает только на сборке.
Три теста `parse_warnings_test.dart` поймали это сразу. Теперь санитайзер
различает исходы: `SanitizeResult.explicitDropNode` — это ЯВНОЕ
`on_invalid: { action: drop_node }`, и только оно отбраковывает при разборе.
Отбраковка неполных записей при разборе — отдельное решение с другой ценой, и
§477 его не принимал.

### Гард сборки и узел `origin.kind: json`

Гард `drop_node` УЖЕ УМЕЛ (`registry_gate.dart`: `res.body == null` → запись в
`report.dropped`, `dropRegistryEntries` убирает её из конфига) — нового кода не
потребовалось, потребовался тест. Их два: дословный JSON-узел с негодным
`encryption` снимается и несёт текст реестра с путём и сырым значением; с годным
— проходит НЕТРОНУТЫМ, как и обещает §455.

### Xray-вход

Не трогали, как и велено: до шага 8 фичи 472 у Xray-узла дословной
sing-box-карты не существует — его `rawSource` это объект Xray, и
sing-box-схеме до него не дотянуться.

Проверено прогоном, и вышло лучше ожидаемого. Xray-узел с негодным
`encryption`:

- **код получает** — `vless_encryption_invalid` с путём и значением, в шторке
  Warnings. Его ставит ВТОРОЙ проход, по `emit()` модели
  (`annotateAllWithRegistry`): Xray-узлы через него по-прежнему ходят, отметки
  «разобран конвейером» у них нет;
- **при разборе не отбраковывается** — вердикт `drop_node` снимает узел только
  на проходе по ДОСЛОВНОЙ карте, а её у Xray-узла нет;
- **в ядро не уезжает** — модель `encryption` читает, значит значение попадает в
  `emit()`, а гард сборки судит все записи независимо от входа и снимает эту.

То есть разница ровно в МОМЕНТЕ отбраковки: узел снимается перед стартом, а не
при разборе, и до тех пор стоит в списке с error-кодом на строке. После шага 8
он начнёт отбраковываться там же, где остальные, — своей работы это не
потребует: правило уже в реестре, и маппер отдаст его санитайзеру вместе со всем
телом. Заметка записана в спеку 472, шаг 8.

### Пометка `ext: "mobile"`

Снята лаунчером у URI-параметра `encryption` как неверная. Нашего кода это не
касается: `ext` не читает ни санитайзер, ни реестр, ни один страж — проверено
грепом по `app/lib` и `app/test`.

### Генератор тел стража §476

Поле с `pattern` — единственное выражение реестра, из которого значение НЕ
вывести: по регулярке образца в общем случае не построить, а `sample` заведомо
не подошёл бы, и `drop_node` снёс бы весь узел — круг не проверил бы ничего.
Образец пишется руками (`_kPatternSamples`, по пути поля), но не молча:
незнакомый путь роняет генерацию `UnsupportedExpression`, и образец,
переставший подходить под выражение реестра, роняет её тоже. Обе проверки держат
таблицу живой при бампе контракта.

### Свои тесты (12)

- `vless_pipeline_invariants_test.dart`, группа §477 — восемь: годные значения
  (ключ 32, padding-блоки, два блока); края обрезаются тихо и в тело едет
  обрезанное; пробел внутри сегмента законен; пусто / точное `none` / ` none ` —
  слоя нет и кода нет; `None`/`NONE` отбраковывают узел; шесть негодных форм
  отбраковывают узел, а не снимают поле; в код уезжает сырое значение до
  обрезки; тело sing-box судится тем же правилом, что и ссылка;
- `registry_invariant_test.dart` — два линтера: диалект `pattern` (запрещённые
  конструкции, якоря, компиляция) и форма `absent_values`;
- `registry_gate_test.dart` — два: дословный JSON-узел с негодным и с годным
  `encryption`.

Корпус: 18 кейсов `uri/vless` + 4 `body/singbox` зелёные без override; ни одно
существующее ожидание не менялось. Identity vless не сдвинулась.

## Коммиты

| Коммит | Что |
|---|---|
| `fix(477)` | `pattern` и `absent_values` в реестре и санитайзере, линтеры, точное `none` в маппере, `drop_node` на JSON-входе, тесты гарда, доки |
