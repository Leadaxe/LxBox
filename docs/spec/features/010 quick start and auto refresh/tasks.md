# 010 — Tasks

- [x] Создать `assets/get_free.json` с подписками и правилами
- [x] Зарегистрировать asset в `pubspec.yaml`
- [x] Реализовать `GetFreeLoader` (load + cache)
- [x] Добавить `applyGetFreePreset()` в SubscriptionController
- [x] Quick Start card на HomeScreen (условие показа, UI, обработчик)
- [x] `parseReloadInterval()` в SettingsStorage
- [x] `shouldRefreshSubscriptions()` + `lastGlobalUpdate` persistence
- [x] `_startWithAutoRefresh()` в HomeScreen
- [x] `setLastGlobalUpdate()` после успешного `updateAllAndGenerate()`
- [x] Добавить `name`, `lastUpdated`, `lastNodeCount` в ProxySource
- [x] Обновить serialization (fromJson/toJson)
- [x] `displayName` в ProxySource (hostname extraction)
- [x] `subtitle` в SubscriptionEntry (статус + time ago)
- [x] Обновить UI подписок (font weight, subtitle, compact chip)
