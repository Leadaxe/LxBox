# 565 — Род группы `selector`: паритет с контрактом и ручной выбор канала

| Поле | Значение |
|------|----------|
| Статус | Фаза A реализована (влита в develop, см. «Итог фазы A»); фаза B = `replace` по патчу контракта (ждём версию) |
| Дата | 2026-09-26 |
| Решение владельца | 26.09.2026: паритет нужен; LxBox учится работать с `selector` как с группой; группа провайдера встаёт вместо себя и появляется как выбор в списке каналов; иногда целую подписку хочется заменить ручным селектором |
| Связанные | PARSING_PRINCIPLES §5, `registry/protocols/group.json` (`genus`), BACKUP `kind: auto` / `group_type` / `default`; §322 (авто-группа), §368 (sing-box-конфиг), §556 §62–§63, §560 п.8 |

## Что это

Группа-узел в подписке бывает двух родов, и род — это сам `entry.type`:
`selector` (человек выбирает члена, поле `default`) и `urltest` (автовыбор
по пингу). Сегодня LxBox знает один род: `AutoSelectSpec` с зашитым
`protocol = 'urltest'`; `selector` при импорте сворачивается в `urltest` с
предупреждением `SelectorAsAutoWarning`, поле `default` только проносится
сквозь бэкап (`manualDefault`), кодек пишет всегда `group_type: urltest`.

Норма контракта (`group.json` → `genus`): sing-box несёт род как есть;
Xray-балансировщик и `autogroup://` — всегда `urltest`. Сторона, не
исполняющая род, обязана пронести `default` (`preserve_unexecuted`). После
этой фичи LxBox род исполняет, и `preserve_unexecuted` остаётся только как
чтение старых записей.

## Как работает после фичи

### Фаза A — род в модели, разборе, эмите, хранении и UI группы

1. **Модель.** У `AutoSelectSpec` появляется род `genus: selector | urltest`
   (значения — из `group.json` `genus.values`, не литералы Dart; читать при
   загрузке реестра). `protocol` = род. `manualDefault` становится активным
   полем рода `selector`: тег выбранного члена.
2. **Разбор.** sing-box `selector` сохраняет род, предупреждение
   `SelectorAsAutoWarning` снимается вместе с тестами на него. Xray
   `balancers[]` и `autogroup://` — `urltest`, как велит `genus.by_source`.
3. **Эмит тела.** `emitRaw`: `type` = род; для `selector` в тело идут
   `outbounds` (состав) и `default`. **Состав в теле результата разбора:**
   группа, чей состав задан источником явно (список тегов в `selector`/
   `urltest` sing-box, `selector` Xray-балансировщика), эмитит эти теги сразу
   при разборе — так ждут оба кейса корпуса; группа папки LxBox с правилом
   набора (`membership`) — как сейчас, состав на сборке. Признак — есть ли у
   группы явный список членов из источника.
4. **Сборка.** `resolveAutoSelectMembers`: для `selector` — состав из явного
   списка, `default` уходит в конфиг ядра; переключение члена в рантайме через
   существующий канал selector-outbound'а ядра (`selectorGroupTags`,
   `home_state.dart`) — проверить, что группа подписки попадает в этот список.
5. **Хранение.** Кодек `auto_group_record.dart` пишет `group_type` по роду и
   `default` для `selector`; чтение старых записей без рода — `urltest`.
   Бэкап — по `backup.schema.json` (`kind: auto` членом папки, `group_type`,
   `default` как NodeLink). Миграции нет: старые записи читаются как прежде.
6. **UI группы** (там, где группы показываются сегодня: `folder_detail_screen`,
   `auto_group_edit_screen`, `outbound_view_screen`): у группы рода `selector`
   вместо параметров urltest — список членов с отметкой текущего `default`,
   выбор члена меняет `default` и пересобирает конфиг. Строки только
   английские. Род показывается меткой рядом с именем группы.
7. **Корпус.** `body/singbox/group_member_missing` и `body/xray/balancer_group`
   зелёные без override. `body_contract_test` — 0 красных.

### Фаза B — свёртка источника в группу (`replace`)

Решение владельца через лаунчер 26.09.2026 (второе, до старта фазы B):
прежняя форма `fold` + `fold_tag` уходит в legacy. Свёртка — настройка папки
или подписки, поле **`replace` внутри записи `sources[]`** (`kind: folder |
subscription`), одинаково в состоянии лаунчера, в бэкапе и у LxBox:

```json
"replace": {
  "mode": "manual" | "auto" | "both",
  "tag": "Proton",
  "auto": { …форма direction.schema.json#/$defs/auto… }
}
```

- Нет объекта — источник не свёрнут, его узлы идут в Направления по одному.
- `tag` — явное имя группы в конфиге ядра.
- `manual` — селектор `tag` из всех узлов источника, члена выбирает
  пользователь в рантайме (selector ядра); `auto` отсутствует.
- `auto` — urltest-группа `tag` с параметрами `auto{…}`.
- `both` — селектор `tag`, у которого первая опция и умолчание — авто-двойник
  `<tag>-auto`.
- Состав = все узлы источника, обновляется вместе с ним; свёртка — объявленное
  корневое имя, выбирается Направлением (D-115, NODE_LINK §8).
- Писатели пишут только `replace`; читатели терпят старые `fold`/`fold_tag`
  (`select` → `manual`, `select_auto` → `both`).
- Провайдерская группа остаётся членом подписки, корневой `kind: auto` под
  запретом; хранимого `default` у свёртки нет.

Как свёртка разворачивается в группы на сборке у лаунчера —
`core/config/folder_replaces.go`; норма придёт патчем контракта (BACKUP.md,
`backup.schema.json`, корпус `backup`, параграф TASKS_LXBOX). Реализация
фазы B стартует после патча, по новой форме, поверх рода из фазы A:
модель источника получает `replace`, сборка разворачивает его в группы
нужного рода, галочка и режим — в настройках папки/подписки, свёртка видна
в Направлениях и в списке каналов одной записью.

## Риски

- Identity узлов-членов не меняется; тег группы — по прежним правилам.
- Группы, уже сохранённые как `urltest` из бывшего `selector`, родом назад не
  станут: истина потеряна при старом импорте; при обновлении подписки род
  придёт заново из источника.
- `default` указывает на тег члена; если член выпал на сборке —
  `group_member_dropped` (1.1.67) и `default` на первого живого, с кодом.

## Верификация фазы A

По одному файлу: `body_contract_test` (0 красных), `contract_test` (378/0),
`backup_corpus_test`, `lx_backup_*` (round-trip записи `selector` с `default`),
тесты `singbox_config_test`, `json_parsers_test`, `server_list_build`-тесты,
`auto_group_folder_test`, виджет-тест выбора члена селектора. Полный прогон — CI.

## Итог фазы A

Коммиты ветки `task-565`: `3dc73e8c` (п.1, п.3 — род в модели, тело по роду),
`89795953` (п.2, п.7 — разбор, корпус), `f78faa6c` (п.4 — сборка),
`2fc5a21a` (п.5 — хранение и бэкап), `bf728e5d` (п.6 — UI), `f24b30d7`
(страж зеркала рода).

Сделано:

- `lib/services/contract/group_genus.dart` — род из `group.json` → `genus`
  (`values`, `by_source`); роль «автовыбор» = `by_source.uri`, «ручной» —
  второе значение `values`. Зеркало на незагруженный реестр сверяет
  `test/contract/group_genus_test.dart`.
- `AutoSelectSpec`: `genus`, `isManual`, `protocol` = род; `manualDefault` —
  активное поле ручного рода; `sourceParamKeys` (параметры, объявленные
  источником) и `sourceMemberTags` (члены Xray-балансировщика) — производные
  от тела, в хранение не идут.
- Тело разбора: `type` = род; `outbounds` — состав источника (ссылки явного
  состава или `sourceMemberTags`); selector — `default`; urltest — только
  объявленные источником параметры. Полные параметры для ядра дописывает
  `coreEntry` на сборке (порядок ключей прежний, golden не сдвинулся).
- Разбор: sing-box несёт род как есть, `SelectorAsAutoWarning`
  (`selector_as_auto`) снят вместе со строкой каталога; Xray — род
  `by_source.xray`. `default` связывается с сырым тегом члена.
- Сборка: `default` → итоговый тег; член не разрешился — поле снимается (ядро
  берёт первого), без дубля кода `group_member_dropped`; `passive_check` только
  у автовыбора; группа любого рода не идёт в urltest-двойник Направления.
- Кодек: `group_type` = род, `default` ручного рода — объект NodeLink, у
  ручного рода нет `strategy`; запись без рода — автовыбор; `default` у
  urltest доживает строкой (`preserve_unexecuted`). Упрощение
  `backup_group_degraded` «selector→urltest» снято.
- UI: редактор группы — режим Manual со списком членов и отметкой `default`
  (параметры замера скрыты), редактор больше не сводит selector к urltest при
  сохранении; строка папки — род и выбранный член; экран узла — Mode Manual и
  отметка выбранного члена.
- Корпус: `body/singbox/group_member_missing`, `body/xray/balancer_group` —
  зелёные без override; `body_contract_test` 167/0, `contract_test` 378/0.

## Нерешённое

- **Override лаунчера в корпусе бэкапа.** `v10_dev_forms`,
  `v10_group_degraded`, `v10_group_links` (`*.expected.lxbox.json`) ждут
  `backup_group_degraded` «selector→urltest, default dropped»; у нас они в
  `_pendingCases` `backup_corpus_test`. Запрос лаунчеру: убрать эту строку из
  трёх override — после этого записи снимаются.
- **Заметка `group.json` → `note` и `round_trip.impl`** описывают старую
  сторону Dart (selector → urltest). Правка — у лаунчера, вместе с
  предыдущим пунктом.
- **Выбор члена у группы подписки** не хранится: группа подписки производна
  от тела, её `default` — провайдерский. Селектор ядра группы подписки
  попадает в `selectorGroupTags` (выбор в рантайме через главный экран),
  но переживает только до перезапуска. Хранение выбора и показ группы как
  канала — фаза B.
- **Xray round_robin в теле разбора** несёт `mode`/`balancer` как
  расширение LxBox; у лаунчера балансировщик всегда urltest без режима
  (`group.json` note). Корпусного кейса на roundRobin нет — сверить при
  согласовании фазы B.
- **Выбор члена из экрана узла** (переключение в рантайме прямо на экране
  группы) не сделан: экран только отмечает выбранного.
- Фаза B (корневая группа-источник, пользовательский селектор поверх
  подписки, список каналов) — не начиналась.
