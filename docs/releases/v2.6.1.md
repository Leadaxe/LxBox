# L×Box v2.6.1

Патч к v2.6.0 — две доводки главного экрана.

**Quick links:**
[🐛 Исправлено](#-исправлено) ·
[✨ Новое](#-новое) ·
[📲 Install](#-install)

---

## 🐛 Исправлено

| # | Было | Стало |
|---|---|---|
| §203 | Значение пинга в обычной строке ноды уезжало в **середину** (регресс v2.6.0) | Пинг снова прижат к правому краю в любой строке |

## ✨ Новое

| # | Что |
|---|---|
| §203 | **Select server** в контекстном меню Auto-ноды (long-press): подсвечивает сервер, который Auto выбрал быстрейшим, и прокручивает к нему. Канал не переключает |

## 🔬 Verified

- `flutter analyze` — **No issues found**, `flutter test` — **1362 passed**.
- Ядро — **`v1.14.0-lx.1-rc.10`** (без изменений).
- Device CPH2411 / Android 15: пинг у правого края; Select server подсвечивает + скроллит к выбранному серверу.

## 📲 Install

```bash
adb install -r LxBox-v2.6.1-arm64-v8a.apk
```

Без uninstall! Поверх существующей установки. Настройки и подписки сохранятся.

---

Предыдущий релиз: [v2.6.0](docs/releases/v2.6.0.md).
