# L×Box v2.3.1

Bugfix-релиз: **AmneziaWG-узел с detour на WireGuard больше не вешает приложение**. Плюс новое ядро sing-box-lx с фиксами AmneziaWG.

**Quick links:**
[🛠 Fixed](#-fixed) ·
[🔬 Verified](#-verified) ·
[📲 Install](#-install) ·
[🇷🇺 На русском](#-lxbox-v231-на-русском)

---

## 🛠 Fixed

**AmneziaWG detour на WireGuard больше не вешает VPN.**

Раньше: если у AmneziaWG-узла в `detour` указан WireGuard-туннель, VPN намертво вис в «Connecting…» — другие узлы не подключались, кнопка не реагировала, помогал только перезапуск приложения. AmneziaWG поверх WireGuard вешает ядро на Android.

Исправлено на трёх уровнях:

- **Ядро** (sing-box-lx `v1.13.13-lx.9`) — такой узел теперь отклоняется с понятной ошибкой в логе вместо зависания. Остальные узлы продолжают работать ([#2](https://github.com/Leadaxe/sing-box-lx/issues/2)). Заодно AmneziaWG-конфиг с `jmin > jmax` отвергается на старте, а не роняет ядро паникой ([#3](https://github.com/Leadaxe/sing-box-lx/issues/3)).
- **UI** — при редактировании AmneziaWG-узла из списка **Detour** убраны все WireGuard-цели (собрать сломанную цепочку больше нельзя), а в строке Protocol теперь видно «AmneziaWG (wireguard)». Уже сохранённый невалидный detour сбрасывается при открытии настроек узла.
- **Приложение** — если туннель всё же завис, VPN-сервис принудительно останавливается по таймауту вместо того, чтобы висеть вхолостую. Кнопка снова работает сразу, без перезапуска приложения.

---

## 🔬 Verified

- `flutter analyze` clean.
- Воспроизведено и проверено на устройстве (Android 13): AmneziaWG-узел с detour на WireGuard больше не вешает VPN, переключение на рабочий узел поднимает соединение мгновенно.
- Ядро — **`1.13.13-lx.9`** (форк sing-box-lx).

---

## 📲 Install

```bash
adb install -r app-release.apk
```

Без uninstall! Поверх существующей установки. Все настройки и подписки сохранятся.

---

## 🇷🇺 L×Box v2.3.1 на русском

- **AmneziaWG больше не вешает приложение** — раньше AmneziaWG-узел с detour на WireGuard намертво вешал VPN (висел в «Connecting…», помогал только перезапуск). Теперь это исправлено: ядро отклоняет такую связку, UI не даёт её собрать, а зависший туннель сам останавливается по таймауту.
- **Новое ядро** sing-box-lx `1.13.13-lx.9` с фиксами AmneziaWG.

Предыдущий релиз: [v2.3.0](docs/releases/v2.3.0.md).
