Core v1.14.2-lx.3: VLESS with Vision and VLESS Encryption connects on any transport incl. XHTTP; XHTTP picks HTTP version from alpn. hysteria2 from 3x-ui with gecko keeps packet sizes. XHTTP keeps uplinkDataPlacement body/auto. sing-box JSON servers and the latency check go through the protocol registry: one bad server no longer fails a check batch. Parsing and the registry guard are about 2x faster. JSON syntax highlighting in the config editor and JSON views.
