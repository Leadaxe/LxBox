name: lxbox
description: L×Box — Android MVP (sing-box + Clash API).
publish_to: 'none'

version: 2.5.0-dev.2+879
# ↑ placeholder. Реальная версия записывается из git tag:
#   • CI release build (.github/workflows/ci.yml → "Inject release version") —
#     `version: ${tag#v}+${git rev-list --count HEAD}` перед `flutter build`.
#   • Локальная сборка (scripts/build-local-apk.sh) — `git describe --tags --abbrev=0`
#     для versionName и `rev-list --count HEAD` для versionCode, revert на EXIT.
# Никогда не редактируется в репо вручную при release-flow (см. docs/RELEASE_PROCESS.md).

environment:
  sdk: ^3.11.4

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  http: ^1.2.2
  cryptography: ^2.7.0 # §025 — X25519 keygen для Cloudflare WARP-регистрации
  crypto: ^3.0.0 # §136 — HMAC-SHA256 для QUIC Initial key derivation
  json5: ^0.8.2
  file_picker: ^11.0.2
  path_provider: ^2.1.5
  shared_preferences: ^2.3.4
  share_plus: ^10.1.4
  freezed_annotation: ^2.4.4
  json_annotation: ^4.9.0
  device_info_plus: ^10.1.2
  package_info_plus: ^8.0.2
  connectivity_plus: ^6.0.5
  collection: ^1.18.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  flutter_launcher_icons: ^0.14.4
  build_runner: ^2.4.13
  freezed: ^2.5.7
  json_serializable: ^6.8.0

flutter:
  uses-material-design: true
  assets:
    - assets/wizard_template.json
    - assets/warp_endpoints.json # §136 — WARP endpoint/SNI random pools
    - assets/icons/app_icon.png
    - assets/icons/cloudflare.png # §025 — официальный логотип Cloudflare (Simple Icons, CC0)

# Иконка из sing-box Launcher (macOS .app Resources/app.icns → PNG 1024²).
flutter_launcher_icons:
  image_path: assets/icons/app_icon.png
  android: true
  ios: true
  macos:
    generate: true
  windows:
    generate: true
  web:
    generate: true
