import '../../models/node_warning.dart';

/// §477 / §484 — вердикт «запись снята при разборе», вынесенный наружу:
/// `null`-ответ конвейера сам по себе о причине не говорит.
///
/// Заполняется санитайзером (`on_invalid: drop_node`) и движком маппера
/// (`required` без значения — `field_missing`).
final class XrayDropVerdict {
  /// Запись снята явным правилом: `drop_node` реестра или обязательная запись
  /// маппера без значения.
  bool explicit = false;

  /// Код и адрес причины — первый `error`-код, который поставил судья.
  RegistryWarning? reason;
}
